# Calendar-and-Emailing
EmailSystem.vue and AgendaDaymap.vue are individual components, you can see the dependencies they utilise within the files. 
You may have to force download dependencies to load them. 
